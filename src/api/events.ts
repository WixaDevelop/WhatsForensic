/**
 * Suscripciones a Channels y eventos globales del backend.
 *
 * En Fase 0 vacío. Fase 1+ agrega:
 * - `subscribeToIngestProgress(evidenceId, cb)`
 * - `subscribeToAnalysisProgress(runId, cb)`
 * - `subscribeToExportProgress(cb)`
 * - `subscribeToErrors(cb)`
 */

export {};
