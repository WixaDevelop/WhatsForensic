//! Detección heurística de fuente a partir de filename + tablas + columnas.
//!
//! TODO fase 3: combina señales (`msgstore.db` ↔ Android, `ChatStorage.sqlite`
//! ↔ iOS, presencia de tablas `Z*` ↔ Core Data) y devuelve una sugerencia
//! con `confidence`. El usuario confirma; nunca se asume.
